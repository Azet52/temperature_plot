<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.stirlitz52.temperatureplot" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".TemperaturePlotActivity"
            android:label="@string/app_name" >
        </activity>

        <service android:name=".measurements.MeasureService" >
        </service>

        <activity
            android:name=".SurfaceDrawActivity"
            android:label="@string/title_activity_surface_draw" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
